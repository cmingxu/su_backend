= form_for [:dashboard, @entity], class: 'uk-form', multipart: true  do |f|
  %table.uk-table.uk-table-stripped.uk-table-hover
    %caption 模型
    %tr
      %td 文件夹
      %td
        = f.select :folder_id, options_from_collection_for_select(current_user.folders, 'id', 'name', f.object.folder_id), class: "uk-form-select"
        = link_to "新文件夹", new_dashboard_folder_path
    %tr
      %td 名称
      %td
        = f.text_field :name, class: 'uk-from-large'
        (确保名称和SU模型内部名称一致)

    %tr
      %td 模型文件
      %td= f.file_field :skp_file

    %tr
      %td.uk-text-right{colspan: 2}
        = f.submit f.object.persisted? ? "修改" : "增加", class: 'uk-button'

